defmodule Stingray.UploadManager.Test do
  use ESpec

  # This is complex to unit test and is tied to a lot of system operations.
  # Mock Nerves firmware is in spec/data/firmware/nerves.fw

  # it "detects uploaded firmware and exposes it on a network share"
end
